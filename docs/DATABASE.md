# Database Foundation

Core entities: units, profiles, awardees, facilitator assignments, assessment periods, versions, modules, dimensions, questions, sessions, answers, results, signals, follow-ups, generated documents, and audit logs.

All public tables use Row Level Security. Migration files are mirrored in `supabase/migrations` and match the initial remote migration history.
